# Указатели
**Указатель** — это переменная, которая хранит адрес другой переменной в памяти.
📦 значение лежит в памяти
📍 указатель знает, где именно

![img_2.png](../memory_ptr/img/ptr.png)

## 🔄 Зачем нужны
- Чтобы **изменять значение** вне текущей функции
- Чтобы **не копировать большие структуры**
- Для более эффективного управления памятью

## 🖇️ Синтаксис
```go
// *int — тип указателя на int (*T)
// &x — оператор взятия адреса, возвращает адрес переменной x
// *p — разыменование, т.е. получение значения, на которое указывает указатель
var x int = 42     // обычная переменная
var p *int = &x    // p — указатель на x, & — взять адрес

fmt.Println(x)  // 42
fmt.Println(p)  // 0xc000014098 (адрес в памяти)
fmt.Println(*p) // 42

// изменение значения через указатель
*p = 100 
fmt.Println(x)  // 100
```

## 🏗️ Указатели в функциях
Передача по значению vs указатель:
```go
func modifyValue(val int) {
    val = 100
}

func modifyPointer(p *int) {
    *p = 100
}

x := 10
modifyValue(x)
fmt.Println(x) // 10 — значение не изменилось

modifyPointer(&x)
fmt.Println(x) // 100 — значение изменилось

```
Функции могут возвращать указатели на локальные переменные. Go автоматически размещает такие переменные в куче, 
а не на стеке.
```go
func createCounter() *int {
	counter := 0
	return &counter // Go поместит counter в кучу, а не в стек
}

func main() {
	c := createCounter()
	fmt.Println(*c) // Выведет 0
	*c++
	fmt.Println(*c) // Выведет 1
}
```

## 🧱 Указатели на структуры и массивы
```go
type Person struct {
    Name string
    Age  int
}

p := Person{Name: "Alice", Age: 25}
pp := &p             // указатель на структуру

pp.Age = 26          // изменение через указатель
fmt.Println(p.Age)   // 26

```

Передача структур: по значению vs по указателю
```go
type Person struct {
Name string
Age  int
}

// передача по значению 
func modifyValue(p Person) {
p.Age = 30
}

// передача по указателю
func modifyPointer(p *Person) {
p.Age = 30
}

func main() {
person := Person{Name: "Alice", Age: 25}

modifyValue(person)
fmt.Println(person.Age) // 25 — оригинал не изменился

modifyPointer(&person)
fmt.Println(person.Age) // 30 — оригинал изменился
}

```

## ⚠️ Указатели и nil
`nil`-указатель нельзя использовать. Перед обращением к указателю нужно проверять его на `nil`
```go
var p *int
fmt.Println(p) // <nil>

if p != nil {
    fmt.Println(*p)
}

```

## 🧠 Особенности Go
- Go автоматически разыменовывает указатели у структур
- Указатели безопасны и контролируются компилятором
- В Go нет ссылок — только указатели

