# Механизмы обработки ошибок в Go
В Go ошибки считаются обычными значениями, которые возвращаются из функций. Это позволяет чётко управлять поведением 
программы в случае возникновения проблем. В отличие от многих других языков, Go не использует исключения для обработки 
ошибок, что делает поток выполнения программы более предсказуемым и понятным.

## Базовый синтаксис обработки ошибок
```go
func divide(a, b int) (int, error) {
if b == 0 {
return 0, errors.New("деление на ноль")
}
return a / b, nil
}

func main() {
result, err := divide(10, 0)
if err != nil {
fmt.Println("Ошибка:", err)
return
}
fmt.Println("Результат:", result)
}
```
## Создание и обертывание ошибок
```go
err1 := errors.New("что-то пошло не так")
err2 := fmt.Errorf("ошибка: %v", originalError)
err3 := fmt.Errorf("ошибка: %w", originalError)
```
## Проверка типа ошибки
```go
//errors.Is(err, os.ErrNotExist): является ли ошибка err — os.ErrNotExist
//или содержит ли она её внутри (через обёртки).
if errors.Is(err, os.ErrNotExist) {
// Обработка ошибки
}

var pathError *os.PathError
//errors.As(err, &pathError): пытается извлечь конкретный тип ошибки из err
if errors.As(err, &pathError) {
fmt.Println("Ошибка пути:", pathError.Path)
}
```
### 💡 Разница между Is и As
| Функция   | Что проверяет                        |
| --------- | ------------------------------------ |
| errors.Is | Равна ли ошибка конкретному значению |
| errors.As | Является ли ошибка конкретным ТИПОМ  |
## Пользовательские типы ошибок
**Пользовательский тип ошибок в Go** — это структура (или любой другой тип), реализующая интерфейс error через 
метод Error() string, созданная для передачи структурированной, детальной информации об ошибке, помимо стандартной
строки. Это позволяет коду анализировать причины, коды ошибок и поля, а не просто сравнивать их строковое представление.
```go
type ValidationError struct {
Field   string
Message string
}

func (e ValidationError) Error() string {
return fmt.Sprintf("ошибка валидации поля %s: %s", e.Field, e.Message)
}
```

### Стандартные способы создания ошибок
В Go существуют стандартные способы создания ошибок:
```go
import (
"errors"
"fmt"
)

// Простая ошибка
err := errors.New("простая ошибка")

// Ошибка с форматированием
errFmt := fmt.Errorf("ошибка с кодом: %d", 404)
```
Эти подходы просты, но часто недостаточны для передачи детальной информации.
### Создание пользовательского типа ошибки
Пользовательские ошибки создаются при помощи реализации интерфейса error:
```go
type InsufficientBalanceError struct {
    Balance int
    Amount  int
}

func (e *InsufficientBalanceError) Error() string {
    return fmt.Sprintf("недостаточно средств: баланс %d, требуется %d", e.Balance, e.Amount)
}
```
Использование указателя (*) помогает избегать копирования и облегчает проверку типа ошибки.
### Использование пользовательских ошибок
```go
func withdraw(balance, amount int) (int, error) {
    if amount > balance {
        return balance, &InsufficientBalanceError{Balance: balance, Amount: amount}
    }
    return balance - amount, nil
}

func main() {
    balance, err := withdraw(100, 150)
    if err != nil {
        var insuffErr *InsufficientBalanceError
        if errors.As(err, &insuffErr) {
            fmt.Println("Ошибка:", insuffErr)
        } else {
            fmt.Println("Неизвестная ошибка:", err)
        }
        return
    }
    fmt.Println("Новый баланс:", balance)
}
```
Использование errors.As() рекомендуется для проверки и извлечения конкретных типов ошибок.
### Оборачивание ошибок (Wrapping errors)
```go
func readConfig(file string) ([]byte, error) {
    data, err := os.ReadFile(file)
    if err != nil {
        return nil, fmt.Errorf("не удалось прочитать файл конфигурации: %w", err)
    }
    return data, nil
}

func main() {
    _, err := readConfig("config.yaml")
    if err != nil {
        if errors.Is(err, os.ErrNotExist) {
            fmt.Println("Файл конфигурации не найден.")
        } else {
            fmt.Println("Ошибка при чтении конфигурации:", err)
        }
    }
}
```
Функция `errors.Is()` позволяет проверять наличие конкретной ошибки в цепочке обёрнутых ошибок.
### Иерархия ошибок
В Go нет классической иерархии ошибок, как в Java или C# (где есть Exception → RuntimeException → ...). Но возможно
сделать иерархию ошибок через композицию (встраивание).
```go
// Это базовая ошибка приложения. 
// Она хранит: 
// Code — код ошибки (например 500)
// Message — описание
// И реализует интерфейс error
type AppError struct {
    Code    int
    Message string
}

// Теперь *AppError — это полноценная ошибка.
func (e *AppError) Error() string {
    return fmt.Sprintf("[%d] %s", e.Code, e.Message)
}

// Здесь используется встраивание (embedding). Это значит:
// DatabaseError содержит AppError внутри и наследует его методы
// То есть DatabaseError тоже автоматически становится error,
// потому что у него есть метод Error() через встроенный AppError.
type DatabaseError struct {
    *AppError
    Query string
}

//Возвращается ошибка типа: *DatabaseError
func queryDB(query string) error {
    return &DatabaseError{
        AppError: &AppError{Code: 500, Message: "Ошибка базы данных"},
        Query:    query,
    }
}
```
## Использование defer
**defer** — это ключевое слово, которое откладывает выполнение функции до момента выхода из текущей функции
```go
func main() {
	//defer говорит: Выполни это в самом конце.
    defer fmt.Println("мир")
    fmt.Println("привет")
}
// Вывод:
// привет
// мир
```
🔹 Несколько defer
Если их несколько — они выполняются в обратном порядке (LIFO).
```go
func main() {
	defer fmt.Println("1")
	defer fmt.Println("2")
	defer fmt.Println("3")
}
// Вывод: 
// 3
// 2
// 1
```
🔹 Важный момент
Аргументы вычисляются **СРАЗУ**.
```go
func main() {
x := 10
defer fmt.Println(x)
x = 20
}
// Вывод: 10
```
## Замыкания в defer
Замыкание — это функция, которая использует переменные из внешней области видимости
```go
func main() {
x := 10
defer func() {
fmt.Println(x)
}()
x = 20
}
```
Go:
✔ не вычисляет x
✔ а сохраняет функцию
✔ которая внутри будет читать переменную x

А функция выполнится в самом конце. К моменту выполнения: x = 20

🔥 Важно понять
Замыкание **НЕ** копирует переменную. Оно **хранит** ссылку на неё.
## Panic и Recover
**panic** — это **немедленная** остановка нормального выполнения программы.
Когда вызывается panic:
* текущая функция прекращает работу
* начинают выполняться все defer
* паника "поднимается вверх" по стеку вызовов
* если её никто не перехватит — программа падает
```go
func main() {
	panic("что-то пошло не так")
}
// Вывод: 
// panic: что-то пошло не так
// goroutine 1 [running]:
// ...
// exit status 2
// Программа завершилась аварийно
```
**recover**() — это функция, которая может перехватить panic и остановить падение программы
⚠ Работает **ТОЛЬКО** внутри defer и в той же горутине. Если вызвать `recover()` вне `defer` — он вернёт `nil`.

```go
// Пример перехвата
func main() {
defer func() {
if r := recover(); r != nil {
fmt.Println("Поймали панику:", r)
}
}()
panic("что-то сломалось")
fmt.Println("Это не выполнится")
}
// Вывод: 
// Поймали панику: что-то сломалось 
// Программа НЕ упала
```
