# 🧩Generics
**Generics** — это способ писать один код, который работает с разными типами:
📌 Без generics → копипаста
```go
func SumInt(a, b int) int {
    return a + b
}

func SumFloat(a, b float64) float64 {
    return a + b
}

```
    📌 С generics → универсально ✨
```go
func Sum[T int | float64](a, b T) T {
    return a + b
}

```
## 🧠 **Как это работает**
* T — параметр типа
* T подставляется при вызове функции

## 📊 Таблица — что есть что
| Элемент          | Что значит        |             |
| ---------------- | ----------------- | ----------- |
| `T`              | параметр типа     |             |
| `[T any]`        | любой тип         |             |
| `[T int          | float64]`         | ограничение |
| `func F[T](v T)` | generic-функция   |             |
| `type Box[T]`    | generic-структура |             |

# 🧩Интерфейсы
**Интерфейс** — это набор методов. Если тип реализует эти методы → он подходит под интерфейс.
📌 В Go не нужно явно писать implements — всё происходит автоматически ✨

## 🧠 Простой пример
```go
type Speaker interface {
    Speak() string
}

// Любой тип с методом Speak() реализует Speaker.
type Human struct{}

func (h Human) Speak() string {
return "Hello"
}

func main(){
var s Speaker = Human{}
fmt.Println(s.Speak())
}
```
## 📦 Зачем нужны интерфейсы
- абстракция
- слабая связанность
- удобное тестирование

## 📊 Основы интерфейсов
  | Понятие            | Что это                      |
  | ------------------ | ---------------------------- |
  | interface          | набор методов                |
  | неявная реализация | без `implements`             |
  | nil interface      | интерфейс без значения       |
  | полиморфизм        | разные типы → один интерфейс |

## Интерфейс vs Generics
| Interface | Generics     |
| --------- | ------------ |
| поведение | типы         |
| методы    | алгоритмы    |
| runtime   | compile-time |
